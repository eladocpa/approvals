[phases.setup]
nixPkgs = [
  "python311",
  "chromium",
  "chromedriver",
  "fontconfig",
  "liberation_ttf",
]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1"
